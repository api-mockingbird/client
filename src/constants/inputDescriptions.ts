export const EXPIRY_NOTICE = 'Expires in 1 hour. Sign up for persistence.';
export const ENDPOINT_NAME = 'A descriptive name.';
export const URL_PATH = 'Do not include hostname.';
export const HTTP_HEADERS = 'Set as JSON object.';
export const RESPONSE_BODY =
  'If parsing into JSON is possible, response body will be returned as JSON object. Otherwise plain string.';
export const TIMEOUT = 'Set timeout for response.';
